#!/bin/bash

# If we were using models, etc - this would be a good script to have

# # Set default migration message with current date if not provided
# MIGRATION_MESSAGE="${1:-Migration on $(date +'%Y-%m-%d')}"

# # Executing migration commands via the Docker container
# sudo docker container exec -it loggerclipper flask db migrate -m "$MIGRATION_MESSAGE"
# sudo docker container exec -it loggerclipper flask db upgrade

# echo "Migration and upgrade complete."
